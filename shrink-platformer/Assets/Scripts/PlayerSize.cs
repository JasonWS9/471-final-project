using UnityEngine;

public class PlayerSize : MonoBehaviour
{

    [SerializeField] private GameObject cam;

    private enum sizeStates
    {
        normalSize,
        shrunkSize
    }


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
